rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // ATTENTION: Règle non sécurisée, uniquement pour le développement
      allow read, write: if true;
    }
  }
}
