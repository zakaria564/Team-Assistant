rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access to all documents for development
    // WARNING: This is not secure for production applications.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
